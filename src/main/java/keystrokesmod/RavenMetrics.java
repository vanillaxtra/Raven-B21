package keystrokesmod;

// bstats placeholder - anonymous usage stats sent on enable
public final class RavenMetrics {
    private RavenMetrics() {}

    public static void register() {
        // stats collected via bstats when backend chart is configured
    }
}
